#include <stdio.h>

int main()
{
	int number, x;

	printf("Enter number: ");
	scanf("%d", &number);

	if(x > 0){
		printf("number is positive %d\n", number);
		}
	if(x < 0){
		printf("number is negative %d\n", number);
	}
	if(x = 0) {
		printf("number is zero %d\n", number);
	}
	return (0);
}
